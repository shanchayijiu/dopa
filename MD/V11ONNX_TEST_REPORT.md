# v11onnx 测试报告

## 1. 测试执行信息

- 执行时间: 2026-02-28
- 命令: `python -m unittest discover -s tests -v`
- 结果: `11/11` 通过
- 通过率: `100%`

## 2. 用例覆盖

- 单元测试:
  - 模型变体识别
  - 模型自动发现（显式路径、目录扫描）
  - 异常路径（文件不存在、后缀错误）
  - 版本/算子集不匹配（opset mismatch）
  - ORT 运行时配置解析（线程、执行模式、内存池）
  - Provider 选择逻辑
- 集成测试:
  - 完整链路（预处理 `read_img` -> 推理 `infer` -> 后处理 `nms_v8`）
  - 指标阈值门禁（差异 `<= 2%`）
  - 退化检测（阈值超限失败路径）

## 3. 性能对比（基准 vs v11onnx）

测试场景: `tests/test_v11onnx_pipeline_integration.py` 的确定性集成样本（24 帧，320x320）。

| 指标 | 基线模型 | v11onnx | 差异(%) |
|---|---:|---:|---:|
| precision | 1.0000 | 1.0000 | 0.0000 |
| latency_ms | 2.2348 | 2.2348 | 0.0000 |
| throughput_fps | 447.4673 | 447.4673 | 0.0000 |
| cpu_percent | 30.6000 | 30.6000 | 0.0000 |
| memory_mb | 3.8635 | 3.8635 | 0.0000 |
| gpu_percent | 0.0000 | 0.0000 | 0.0000 |

阈值结论: 所有指标差异均 `<= 2%`。

## 4. 对比图（ASCII）

### 差异占比图（阈值 2%）

| 指标 | 差异 | 图示 |
|---|---:|---|
| precision | 0.00% | `|` |
| latency_ms | 0.00% | `|` |
| throughput_fps | 0.00% | `|` |
| cpu_percent | 0.00% | `|` |
| memory_mb | 0.00% | `|` |
| gpu_percent | 0.00% | `|` |

> 注: 当前集成测试采用可复现的确定性样本，确保 CI 稳定；线上真实模型可使用 `v11onnx_benchmark.py` 复核实测指标。
